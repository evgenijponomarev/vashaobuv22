export default function Contacts() {
  const B = 'contacts';

  return (
    <div className={B}>
      Карта, адрес, фотки.

      <style jsx>
        {`
        .${B} {
          
        }
        `}
      </style>
    </div>
  );
}
